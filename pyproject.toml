[project]
name = "concept-benchmark"
version = "0.2.0"
description = "Synthetic benchmarks for evaluating Concept Bottleneck Models."
license = "MIT"
readme = "README.md"
authors = [
    { name = "Julian Skirzynski" },
    { name = "Harry Cheon" },
    { name = "Shreyas Kadekodi" },
    { name = "Meredith Stewart" },
    { name = "Berk Ustun" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

requires-python = ">=3.10"

dependencies = [
    "numpy>=2.2.6,<3.0",
    "pandas>=2.3.1,<3.0",
    "scikit-learn>=1.7.0,<2.0",
    "scipy>=1.15.3,<2.0",
    "tqdm>=4.67.1,<5.0",
    "dill>=0.3.8,<1.0",
    "matplotlib>=3.10.3,<4.0",
    "psutil>=7.0.0,<8.0",
    "torch>=2.7.1,<3.0",
    "pyarrow>=21.0.0,<22.0",
    "fastparquet>=2024.11.0,<2026.0.0",
    "datasets>=4.0.0,<5.0",
    "pillow>=10.0.0,<12.0",
    "skorch>=1.2.0,<2.0",
    "torcheval>=0.0.7,<1.0",
    "torchvision>=0.23.0,<1.0",
    "albumentations>=1.0.0,<2.0",
    "colorir>=2.0.0,<3.0",
    "pero>=0.18.0,<1.0",
    "pycairo>=1.28.0,<2.0",
    "transformers>=4.55.2,<5.0",
    "cvxpy>=1.4.0,<2.0",
    "pyyaml>=6.0,<7.0",
    "open-clip-torch>=2.20.0,<3.0",
    "google-genai>=1.0.0,<2.0",
]

[project.urls]
Repository = "https://github.com/ustunb/concept-benchmark"

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "jupyter",
    "ipykernel",
    "rich",
]

[tool.uv]
default-groups = ["dev"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
exclude = ["data/", "results/", "scripts/", ".claude/"]

[tool.hatch.build.targets.wheel]
packages = ["concept_benchmark"]

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = [
    "E402",   # module-import-not-at-top (intentional lazy imports)
    "E501",   # line-too-long (research code, not worth reformatting)
    "E731",   # lambda-assignment (used in data pipelines)
    "E701",   # multiple-statements-on-one-line-colon
    "E702",   # multiple-statements-on-one-line-semicolon
    "F841",   # unused-variable (readability assignments in drawing/data code)
    "W291",   # trailing-whitespace
]

[tool.pytest.ini_options]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::sklearn.exceptions.UndefinedMetricWarning",
]
testpaths = [
    "tests"
]
